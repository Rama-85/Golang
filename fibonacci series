package main

import (
	"fmt"
)

func main() {
	fmt.Println(0)
	for i := 1; i <= 10; i = i + 1 {
		fmt.Println(fib(i))
	}
}

func fib(n int) int {
	if n <= 1 {
		return n
	} else {
		return fib(n-1) + fib(n-2)
	}

} 

